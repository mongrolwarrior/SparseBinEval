"""sbeval — a small evaluation/benchmark harness for SparseBinSOM.

Stripped-down successor to sombench: drive the `sbsom` CLI over parameter sweeps and
record results. The first sweep is map size (square maps); see sbeval.mapsize.
"""
